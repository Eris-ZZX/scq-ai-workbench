/**
 * Legacy compatibility stub. DingTalk application tokens are no longer
 * available under the current security policy.
 */
export async function getCorpAccessToken(): Promise<null> {
  return null;
}

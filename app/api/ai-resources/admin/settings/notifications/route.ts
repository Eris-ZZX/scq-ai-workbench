/**
 * Canonical generic endpoint. The legacy /settings/dingtalk endpoint remains
 * as a compatibility alias for existing bookmarks and audit links.
 */
export {
  GET,
  PUT,
  POST,
} from '../dingtalk/route';

/** Auth-related constants safe for client and server imports (no cookies/headers). */

/** Post-login landing: choose workbench vs AI resource library */
export const DEFAULT_AFTER_LOGIN = '/portal';
